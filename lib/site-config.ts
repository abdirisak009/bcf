/** Physical office — shown in header, footer, contact, and training pages */
export const OFFICE_ADDRESS = 'Waberi Mall, First Floor, Mogadishu, Somalia';

/** Main brand logo: `/public/logo.png` (header, footer). Certificate PDFs fetch the same asset via `{PUBLIC_SITE_URL}/logo.png` when `CERTIFICATE_LOGO_URL` is unset. */
export const SITE_LOGO_SRC = '/logo.png';

/** Facebook Page — home “latest posts” + footer/social (numeric ID from page URL). */
export const FACEBOOK_PAGE_ID = '61575201336341';

export const FACEBOOK_PAGE_URL =
  'https://www.facebook.com/profile.php?id=61575201336341';
