# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
.env*.local
.env
# Production secrets (copy from .env.production.example on VPS; do not commit real keys)
.env.production
# Deployment JSON (copy from deployment.config.example.json)
deployment.config.json

# Common ignores
node_modules
.next/
.DS_Store

# User-uploaded files (keep folders)
public/uploads/news/*
!public/uploads/news/.gitkeep
public/uploads/publications/*
!public/uploads/publications/.gitkeep
public/uploads/certificates/*
!public/uploads/certificates/.gitkeep

# Local PDF extraction tooling (pip --target)
.pdf-tools/
